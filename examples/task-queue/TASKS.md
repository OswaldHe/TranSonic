# Release Notes Queue

Complete these tasks in order:

- [ ] `release_notes/parser.py`: Parse entries written as `type: message`.
- [ ] `release_notes/grouping.py`: Group parsed entries by type while preserving order.
- [ ] `release_notes/render.py`: Render grouped entries as Markdown release notes.
